const id = 456;
const available = true;
const count = 42;
const name = "Calculation in Physics book";
const author = "Aghogho";

const assignedObject = {
    id: Number(id),
    available: Boolean(available),
    count: Number(count),
    name: String(name),
    author: String(author)
};

console.log(assignedObject);
